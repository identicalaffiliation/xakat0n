# MVP-скоуп: Кейс 2 «Авито Очередь»

## Оценка capacity команды

```
Capacity = 4 человека × N рабочих дней × ~5.5 продуктивных часов × 0.7
```

| Срок | N (раб. дней) | Capacity |
|---|---|---|
| 1 неделя | ~5 | 4 × 5 × 5.5 × 0.7 ≈ **77 часов** |
| 2 недели | ~10 | 4 × 10 × 5.5 × 0.7 ≈ **154 часа** |

Подставьте точное число рабочих дней до дедлайна и пересчитайте.

## Что берём в MVP

Все 16 Must + все 3 Should из `backlog-priorities.md` — 19 пунктов. При заявленной capacity (77–154 часа) укладывается даже по нижней границе: большинство Must завязано на один модуль (`internal/queue`), а не размазано по несвязанной функциональности.

## Could-have — порядок, если останется время

1. ИИ-подбор похожих товаров через эмбеддинги — усиливает FR10, даёт бонус за интеграцию ИИ
2. Live-обновление через WebSocket/SSE
3. Админка для настройки таймера и порога дефицита

## Won't have

См. `baseline-system.md` — регистрация, оформление заказа, оплата, списание стоков вне скоупа кейса.

## Проверка на demo journey

Каждый Must-пункт участвует хотя бы в одном шаге живого демо-пути или явно помечен «Реализовать + тест» в каталоге сценариев (`demo-scenario.md`) — расхождений нет.

---
Скоуп зафиксирован: [дата]. Изменения — только через капитана, по правилу «добавляем одно — убираем другое».
